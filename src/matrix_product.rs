// defines the product of two matrices

//TODO: define struct for matrix
//TODO: define functions for matrix manipulation

pub struct Matrix {
    n_rows: i32,
    n_cols: i32,
    n_elements: i64,
    data: Vec<f32>,
}

impl Matrix {
    fn new(rows: i32, cols: i32) -> Matrix {

    }
}

pub fn run() {
    let mut m: Matrix;

    println!("{}", m.n_elements);


}